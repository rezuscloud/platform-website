package sections

templ GetStarted() {
	<section id="getstarted" data-reveal class="py-12 sm:py-16 bg-paper dark:bg-next-black">
		<div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
			<div class="text-center mb-12">
				<div class="w-12 h-1 bg-accent-gold dark:bg-next-teal-hi mx-auto mb-4"></div>
				<h2 class="text-3xl sm:text-4xl font-bold mb-4 font-mac text-ink dark:text-next-white">Start Your Cloud</h2>
				<p class="text-lg text-ink-muted dark:text-next-subtle max-w-2xl mx-auto font-mac-body">
					The hardware is real. The public setup flow is still loading.
				</p>
			</div>
			<div class="max-w-lg mx-auto space-y-6">
				<div class="bg-next-black dark:bg-next-black overflow-hidden border border-ink dark:next-sunken mac-window-shadow dark:shadow-none">
					<div class="mac-title-bar-animated dark:bg-next-mid flex items-center gap-1.5 px-3 py-2 border-b border-ink dark:next-raised">
						<span class="w-2.5 h-2.5 border border-ink-muted dark:hidden" style="background:#cc3333"></span>
						<span class="w-2.5 h-2.5 border border-ink-muted dark:hidden" style="background:#ccaa00"></span>
						<span class="w-2.5 h-2.5 border border-ink-muted dark:hidden" style="background:#339933"></span>
						<span class="ml-2 text-xs text-ink-muted dark:text-next-subtle font-mac hidden dark:inline">TERMINAL</span>
						<span class="ml-2 text-xs text-ink-muted font-mac dark:hidden">terminal</span>
					</div>
					<div data-terminal data-base-delay="300" data-line-delay="350" class="p-6 space-y-2 font-mac-mono text-sm">
						<p data-line class="text-ink-muted dark:text-next-subtle">
							<span class="text-accent-gold dark:text-next-teal">*</span> SYSTEM CHECK: ALL CLEAR
						</p>
						<p data-line class="text-ink-muted dark:text-next-subtle">
							<span class="text-accent-gold dark:text-next-teal">*</span> HARDWARE: FOUND
						</p>
						<p data-line class="text-ink-muted dark:text-next-subtle">
							<span class="text-accent-gold dark:text-next-teal">*</span> NETWORK: READY
						</p>
						<p data-line class="text-ink-muted dark:text-next-subtle">
							<span class="text-accent-gold dark:text-next-teal">*</span> PUBLIC INSTALLER: LOADING<span class="animate-blink">...</span>
						</p>
						<p data-line class="text-ink-muted dark:text-next-subtle mt-4">
							<span class="text-accent-gold dark:text-next-teal">$</span> <span class="animate-blink text-ink dark:text-next-white">_</span>
						</p>
					</div>
				</div>
				<p class="text-center text-sm text-ink-muted dark:text-next-subtle font-mac-body">
					Coming soon: public setup docs, source access, and the full boot sequence.
				</p>
			</div>
		</div>
	</section>
}
